version: '3.8'
services:
  score-fetcher:
    build:
      context: ./score-fetcher
    volumes:
      - ~/.aws:/root/.aws:ro
    environment:
      - AWS_DEFAULT_REGION=us-east-1
    restart: unless-stopped
