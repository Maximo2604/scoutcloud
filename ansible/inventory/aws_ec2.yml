plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  tag:Project: scoutcloud
  instance-state-name: running
keyed_groups:
  - key: tags.Environment
    prefix: env
  - key: tags.Name
    prefix: name
